#include "holberton.h"

/**
 * spaces - function that count the spaces in the argument.
 * @spc: Argument
 * Return: number of the spaces
 */

int spaces(const char *spc)
{
    int i = 0;

    while (spc[i] == ' ')
    {
    }
    return (i);
}