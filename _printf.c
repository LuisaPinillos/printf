#include "holberton.h"

/**
 * _printf -
 * @format: 
 * Return: 
 */

int _printf(const char *format, ...)
{
  int count_format = 0;
  va_list args;
  va_start (args, format);
  
  if (format != NULL && *format != '\0')
   {
      while (*format != '\0')
      {
        {
        }
      }
   }
}